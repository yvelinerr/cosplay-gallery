---
type: concept
title: "项目开发约定"
summary: "开发入口、任务日志、断联恢复、数据边界及 Index.md 同步维护规则。"
tags: [development, conventions]
created: 2026-09-05
updated: 2026-09-05
source_count: 5
related: ["[[Index]]"]
status: active
scope: "cosplay-gallery 全仓库"
---

# 项目开发约定

## 概述

开始开发时依次阅读本文件、[Index.md](Index.md)、[README.md](README.md)，再打开目标模块。索引用于定位，实际行为以当前源码为准；发现差异时同步更正文档。

当前架构为 Python `requests` 爬虫、标准库 `ThreadingHTTPServer` 和原生 HTML/CSS/JavaScript 单页。没有前端构建步骤，也没有现成的测试、格式化或 CI 配置。保持现有结构，新增依赖或拆分模块时说明必要性。

## 规则

### Index.md 必须随项目变更同步

- 每次项目变更都必须检查并同步更新根目录 `Index.md`，与实现放在同一次交付或提交中。不得将索引更新留作后续任务。
- 新增、删除、移动、重命名文件：更新文件树、职责表、快速定位入口和链接。生成数据只记录目录模式，不逐个列出图片、缓存或令牌文件。
- 修改 HTTP 路由：更新方法、路径、鉴权、参数默认值、响应字段、错误状态、副作用及前端调用点。
- 修改爬虫公共方法、索引结构、状态机、CLI、环境变量、启动方式或依赖：同步更新对应契约和使用说明。影响用户操作时同时更新 `README.md`。
- 修复缺陷后更新或删除索引中的相关限制；尚未验证的行为必须明确标注，不能把预期当成现状。
- 更新 `Index.md` 的 `updated` 日期及末尾维护记录，简述本次变化。即使只是内部重构、样式或文案变更，也记录“已检查，结构/接口无变化”及涉及文件。
- 修改本约定时同步更新索引中的开发文档入口；`AGENTS.md` 保持简短，只指向本文件，避免两套规则漂移。

### 开发日志与断联恢复

- 从下一次开发任务开始，实施前必须在根目录 `dev_log/` 创建独立任务目录。本次建立规则的任务按用户要求不创建任务日志。
- 目录命名为 `dev` + 数字 + 任务名称，例如 `dev1图集搜索优化`。数字从 1 开始，取现有任务目录最大编号加 1；编号唯一，不覆盖、不复用、不重新编号。任务名称应简短明确，并避免 Windows 文件名非法字符。
- 每个任务目录必须包含 `plan.md`、`task.md`、`context.md`。同一任务断联后续做时复用原目录，不因新会话或重连创建重复任务。
- `plan.md` 记录整体规划：目标、范围、实施方案、阶段顺序、依赖与验收标准；方案改变时同步更新。
- `task.md` 记录细分任务完成情况：将工作拆为可独立核验的清单，使用 `- [ ]` / `- [x]` 标记待办和已完成项，同时记录当前步骤、下一步、阻塞、验证结果及更新时间。
- **task.md 必须实时维护：每完成一项，立即更新该项状态与结果并保存，再开始下一项。不得等整个任务结束后批量补记。** 开始某项、遇到阻塞、验证失败、范围调整或交接暂停时也立即更新当前状态；未验证的项目不得标记为已完成。
- `context.md` 保存恢复开发所需的上下文：用户需求与限制、相关文件和接口、已确认决策及原因、环境与命令、关键证据和未解决问题。上下文变化时同步更新，不记录令牌或其他敏感信息。
- 断联恢复时，先读 `AGENT.md`、`Index.md`，再读对应任务的 `context.md`、`plan.md`、`task.md`；核对实际文件、Git 差异和已有验证结果，从第一个未完成项继续。若操作已落盘但日志未更新，先核验并补记，避免重复执行。
- 新建任务目录时同步登记 `Index.md` 的任务入口；每次日志内容或状态变化也同步索引中的相关摘要或维护记录。完成时保留三文档，记录最终结果和遗留事项，供后续检索。
- `dev_log/` 内的 Markdown 开发日志纳入版本控制，不属于忽略的运行日志。目录说明见 [dev_log/README.md](dev_log/README.md)。

### 数据与运行边界

- `data/index.json` 是业务索引，和开发文档 `Index.md` 完全不同。备份业务数据需要同时保存索引和 `data/images/`。
- 保留 `threading.RLock` 的可重入语义；索引读改写、下载、删除和后台任务的变更必须考虑线程并发。
- 保留索引临时文件加 `os.replace` 的原子替换，以及图片 `.part` 下载后替换流程。不要将原子替换误认为跨文件事务或多进程锁。
- 保留已有文章与删除名单的跳过规则。删除会实际移除图片；恢复只解除屏蔽、允许后续重爬，不能向用户承诺恢复本地文件。
- 磁盘统计当前是首次惰性扫描后增量维护；不得在每次统计请求中增加全盘遍历。修改计数逻辑时验证首次加载、下载复用和删除场景。
- 不提交 `data/`、令牌、分享信息、运行日志或缓存，遵循 `.gitignore`；`dev_log/` 的 Markdown 开发日志应纳入版本控制。验证删除、恢复和索引迁移时使用临时数据，避免操作用户图库。
- 导入 `server.py` 会构造全局爬虫并创建数据目录；运行服务器会打开浏览器。`start-share.bat` 会尝试修改防火墙规则，普通验证优先使用隔离的本机环境。
- 修改鉴权时同时检查 API 和图片请求；`/` 与 `/index.html` 当前公开。令牌不区分只读与管理权限，持有者可以触发爬取和删除。

### 验证与完成标准

- 文档变更：检查所有本地链接、文件树与实际文件、路由与源码一致性，执行 `git diff --check`，检查 `git status --short`。
- Python 变更：至少执行语法检查，再按影响范围验证相关行为。仓库当前没有现成测试套件，不得声称“全部测试通过”。
- HTTP 变更：用临时数据验证正常请求、缺少令牌、无效参数、不存在资源；涉及写接口时检查响应和持久化结果。
- 前端变更：在本地服务上检查相关流程与桌面/移动布局；详情、灯箱、筛选、分页、令牌门控和错误响应按影响范围验证。
- 爬虫解析变更：优先使用本地 HTML 样例；涉及下载或删除时用临时目录和受控输入验证。记录实际执行的检查及未验证部分。
- 交付前确认 `Index.md` 已同步，本次任务的三文档齐全且 `task.md` 状态与实际结果一致（用户明确免建日志的任务除外），且变更中没有意外的数据、缓存或敏感信息。

## 示例

新增列表筛选参数时，同时修改 `server.py` 的 `_api_posts` 和 `static/index.html` 的调用逻辑，更新 `Index.md` 的参数表、前端入口和维护记录，并验证筛选组合、分页与空结果。涉及用户用法的变化同步更新 `README.md`。

## Related

- [[Index]]：项目结构、接口与维护记录；普通 Markdown 入口见 [Index.md](Index.md)。
